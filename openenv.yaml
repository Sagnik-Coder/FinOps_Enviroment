name: FinOpsEnv
description: Adversarial Cloud FinOps Benchmark
version: 1.0.0
start_command: server.app:main
port: 7860

endpoints:
  reset: /reset
  step: /step

tasks:
  - id: easy
    name: Easy
    grader: /grader
  - id: medium
    name: Medium
    grader: /grader
  - id: hard
    name: Hard
    grader: /grader

docker:
  build_context: .
  dockerfile: Dockerfile
